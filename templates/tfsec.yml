parameters:
- name: workingDirectory
  type: string

steps:
- powershell: |
    mkdir security-artifacts\tfsec -Force
    tfsec ${{ parameters.workingDirectory }} `
      --format json `
      --out security-artifacts\tfsec\tfsec-report.json
  continueOnError: true
