parameters:
- name: workingDirectory
  type: string

steps:
- powershell: |
    mkdir security-artifacts\checkov -Force
    checkov -d ${{ parameters.workingDirectory }} `
      -o json `
      --output-file-path security-artifacts\checkov
  continueOnError: true
