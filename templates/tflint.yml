parameters:
- name: workingDirectory
  type: string

steps:
- powershell: |
    mkdir security-artifacts\tflint -Force
    tflint --init
    tflint --format json `
      --out security-artifacts\tflint\tflint-report.json `
      ${{ parameters.workingDirectory }}
  continueOnError: true
